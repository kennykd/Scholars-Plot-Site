name: scholars-plot-site

services:
  app:
    image: ${DOCKER_USERNAME:-local}/scholars-plot-site:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3026}
        NEXT_PUBLIC_API_DOCS_ENABLED: ${NEXT_PUBLIC_API_DOCS_ENABLED:-true}
        NEXT_PUBLIC_FIREBASE_API_KEY: ${NEXT_PUBLIC_FIREBASE_API_KEY}
        NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN}
        NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${NEXT_PUBLIC_FIREBASE_PROJECT_ID}
        NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET}
        NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID}
        NEXT_PUBLIC_FIREBASE_APP_ID: ${NEXT_PUBLIC_FIREBASE_APP_ID}
        NEXT_PUBLIC_VAPID_PUBLIC_KEY: ${NEXT_PUBLIC_VAPID_PUBLIC_KEY}
    # environment:
    #   FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID}
    #   FIREBASE_CLIENT_EMAIL: ${FIREBASE_CLIENT_EMAIL}
    #   FIREBASE_PRIVATE_KEY: ${FIREBASE_PRIVATE_KEY}
    env_file:
      - .env.production
    ports:
      - "3026:3026"
    restart: unless-stopped

  db-schema-sync:
    profiles: ["migrate"]
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    # environment:
    #   FIREBASE_PROJECT_ID: ${FIREBASE_PROJECT_ID}
    #   FIREBASE_CLIENT_EMAIL: ${FIREBASE_CLIENT_EMAIL}
    #   FIREBASE_PRIVATE_KEY: ${FIREBASE_PRIVATE_KEY}
    env_file:
      - .env.production
    restart: "no"